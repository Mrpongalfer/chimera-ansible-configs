# Placeholder for docker-compose.yml.j2 created by Chimera v2.7
# WARNING: This is NOT a functional or complete Prometheus configuration.
# Review and replace this content with your actual requirements via Git.
version: '3.7'
services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus_placeholder
    ports:
      - "{{ prometheus_port | default('9090') }}:9090"
    command:
      - '--config.file=/etc/prometheus/prometheus.yml' # Assumes prometheus.yml template exists and is mounted
    # volumes: # Add volume mounts for config and data as needed in real template
    #   - ...:/etc/prometheus/prometheus.yml:ro
    #   - ...:/prometheus
    restart: unless-stopped
