# File: roles/prometheus/templates/docker-compose.yml.j2
# Version: Cleaned for Syntax/Indentation Check

version: '3.7'

volumes:
  prometheus_data: {}

networks:
  monitoring_net:
    driver: bridge

services:
  prometheus: # Start of service definition (2 spaces indent)
    image: prom/prometheus:latest
    container_name: prometheus
    restart: unless-stopped
    volumes: # (4 spaces indent)
      # Mount config (read-only)
      - /etc/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      # Mount data volume
      - prometheus_data:/prometheus
    command: # (4 spaces indent)
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-lifecycle'
    ports: # (4 spaces indent)
      # Use override variable from site.yml, default to 9090 if not set
      - "{{ prometheus_port_override | default('9090') }}:9090"
    networks: # (4 spaces indent)
      - monitoring_net
    labels: # (4 spaces indent)
      org.label-schema.group: "monitoring"
      managed-by: "ansible-chimera-prime"

#  node_exporter: # Example commented out service (2 spaces indent)
#    image: prom/node-exporter:latest
#    container_name: node_exporter
#    restart: unless-stopped
#    # ... other config ...
